# Contributing

Keep changes small, typed, deterministic, and covered by tests. Never add real
health, nutrition, routine, schedule, device, or provider data; use synthetic
fixtures instead. Preserve source provenance and authority boundaries. Run the
backend tests, Ruff, strict mypy, Swift harness, secret scan, and
`git diff --check` before opening a pull request.
