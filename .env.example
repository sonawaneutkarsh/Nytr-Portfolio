# Local-only example values. Never commit real credentials.
HEALTH_DATABASE_URL=
SUPABASE_URL=https://example.supabase.co
SUPABASE_JWT_AUDIENCE=authenticated
SUPABASE_JWT_SECRET=
GEMINI_API_KEY=
# Keep this override empty unless the selected model is reviewed for availability and terms.
GEMINI_MODEL=gemini-2.5-flash
OPEN_FOOD_FACTS_USER_AGENT=Nytr/portfolio (contact: example@example.invalid)
STACKS_INGESTION_MODE=blocked
